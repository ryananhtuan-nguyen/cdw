generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder", "strictUndefinedChecks", "driverAdapters"]
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}
