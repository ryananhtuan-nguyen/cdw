/** @type {import('tailwindcss').Config} */
export default {
  plugins: {
    '@tailwindcss/postcss': {}, // Tailwind’s PostCSS plugin
    // autoprefixer or others, if needed:
    // 'autoprefixer': {},
  },
}
